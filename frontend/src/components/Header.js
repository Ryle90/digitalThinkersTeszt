import { Button } from 'react-bootstrap';

export default function Header () {
    return (
        <header>
            <Button>Get an othor random cocktail</Button>
            <Button>Search cocktail by name</Button>
        </header>
    )
}